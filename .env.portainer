# Environment variables for Portainer Standalone Voice Stack
# Copy these variables into your Portainer Stack environment variables

# Server configuration
SERVER_NAME=matrix.byte-box.org
POSTGRES_PASSWORD=synapse_password
REGISTRATION_SECRET=change_this_secret_key
TURN_SECRET=change_this_turn_secret

# Port configuration (customize if ports conflict)
SYNAPSE_PORT=8008
ELEMENT_PORT=8080
TURN_PORT=3478
TURNS_PORT=5349

# Registration Token (REQUIRED for user registration)
# Set this to a secure token that users need to register
# Example: family2024, friends_welcome, etc.
REGISTRATION_TOKEN=

# TURN server settings
TURN_USERNAME=turn_user
TURN_PASSWORD=turn_password

# Optional: External IP for TURN server (leave empty for auto-detect)
EXTERNAL_IP=
